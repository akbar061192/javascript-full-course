// array insertion and deleting values

let fruits = ['apple', 'grapes', 'oranges'];
console.log(fruits);

// push , unshift
// pop  , shift

// push -- add a new element at the ending to an array
// unshift -- add a new element at the begining of an array
// pop -- removes the last element of an array
// shift -- removes the first element of an array

// fruits.push('bananas');
// let removedFruit = fruits.pop();
// console.log(removedFruit);

// fruits.unshift('strawberry');

// fruits.shift();

fruits.push('bananas');
fruits.push('mangoes');
console.log(fruits);

fruits.unshift('guava');
fruits.unshift('berries');

console.log(fruits);
