let myName = 'John';
console.log(myName);

// null - > used to store value of nothing in a variable.
let age = null;
console.log(age);

// age = 20;

// console.log(age);

// you are just declaring a variable and not storing anything.
let job;
console.log(job);
// job = 'engineer';
// console.log(job);

const info = null;
console.log(info);

// const place;

const isValid = true;

// typeof
// used to find the type of a variable
console.log(typeof myName);
console.log(typeof age);
// above age will return type as object which is a bug in javascript it should be null data type
console.log(typeof job);
console.log(typeof isValid);
