const name = 'My name is Joe';
const age = 'and my age is 29';
const job = 'and i work for xyz company';

const info = name + ' ' + age + ' ' + job;
console.log(info);

// template strings -- used to contact more varaiables in a good fashion.

const output = `${name} ${age} ${job}`;
console.log(output);
